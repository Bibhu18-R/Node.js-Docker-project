# Node.js Service with Basic Auth

A simple Node.js service with two routes:

- `/` - Returns "Hello, world!"
- `/secret` - Protected by Basic Auth, returns secret message

## Setup

1. Install dependencies:
   ```bash
   npm install